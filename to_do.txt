Definir:

savoir expliquer : 

-Namespace
-Template
-friend
-enable if
-lexicographical compare
-explicit
-virtual



Verifier : 
--> chaque fonction
--> Si fonction a des execptions








_Namespace:


_Template:
Defini une classe ou une fonction qui pourra manipuler n'importe quel TYPE de
variable et dont le comportement sera similaire.
Keyword : class
Keyword : typename
typename == class
	fonction :
template <typename anyType>
	anyType GetMAx(anyType a, anyType b)
{
	return (a > b ? a : b);
}
	
	class:
//Definition
template <typename anyType>
	class myClass
{
	anyType [2] //;
}
//Use
main 
{
	//creer un myClass avec INT comme anyType;
	myClass<int> a;
}

friend: 
permet a une fonction NON MEMBRE d'acceder aux attributs public et prive d'une classe
class  exemple
{
	friend friend_function_name(parameters);
};

void	friend_function_name(paramaters)
{
	actual code here;
}

-declarer une fonction FRIEND DANS la classe la fait elle devenir non membre ?


enable_if:

is_integral >

equal/lexicographical compare

std::pair

std::make_pair:

_allocator:


_iterator:


_explicit:


_virtual:
